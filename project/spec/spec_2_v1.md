# feature: improve UI and fix some bugs.

## adjustments
- Model explanations must be in pt-br
- Titles and texts must be in pt-br
- Avoid icons
- Instead of having a button in the sidebar for navigation, leave only the page name so we can navigate between them.
- Add the option to return in the training stages
- After training, return the metrics in a more visual tabular format (bring all the metrics and not just the accuracy)
- Add a short 1-line explanation for each metric
